import { useState } from "react";

function Total({totalE,TotalIn}) {
    return (<>
    <h2 style={{
        margin:'45px 100px'
    }} class="text-primary">Total:</h2>
    </>);
}

export default Total;